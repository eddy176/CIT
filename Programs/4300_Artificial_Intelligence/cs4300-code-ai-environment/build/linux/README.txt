To build/install the system:

$ make configure
$ make 


To remove extra object files, emacs backups, etc,
But leave the installed libraries and executables:

$ make clean


To remove all replacable files (objects, libraries,
executables, dependencies):

$ make spotless
